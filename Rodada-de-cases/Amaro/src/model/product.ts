export interface Product {
    id: string;
    name: string;
    tags: string;
};

export interface ProductInputDTO {
    name: string;
    tags: string;
};

export interface Search {
    id: string,
    name: string,
    tag: string
};

